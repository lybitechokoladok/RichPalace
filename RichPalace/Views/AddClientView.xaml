<UserControl x:Class="RichPalace.WPF.Views.AddClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:components="clr-namespace:RichPalace.WPF.Components"
             xmlns:local="clr-namespace:RichPalace.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Add Client"
                   Grid.Row="0"
                   Style="{StaticResource Header}"/>

        <components:ClientDetailsForm Grid.Row="1" Margin="0 20 0 0"
                                      DataContext="{Binding ClientDetailsFormViewModel}"/>
    </Grid>
</UserControl>
